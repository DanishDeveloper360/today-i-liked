import { ExampleComponent } from '../../main/js/ExampleComponent';

xdescribe(`ExampleComponent API Tests`, () => {
  beforeAll(() => {
    // TODO: add one-time setup code
  });

  beforeEach(() => {
    // TODO: add per-test setup code
  });

  test(`My test description`, () => {
    let myExample = new ExampleComponent();
    console.log(myExample);
    // TODO: add some expectations
  });
});
